#include<iostream>

/*

#define bir makro tanımıdır.

kod derlenemden önce preprocessor tarafından metin değiştirme yapılır.

#define PI 3.14 gibi
Bellekte yer kaplamaz.

parametre alabilir 
#define KARE(x) ((x)*(x))


Makro fonksiyonlar: 
#define MAX(a,b) ((a) > (b) ? (a) : (b))

Tür güvenliği yoktur.
================================================

typedef: yeni tür ismi tanımlamadır.
Compiler time da çalışır.

typedef unsigned int uint;

Artık:

uint x = 5;


uint yeni bir tür değil, unsigned int için takma isimdir.

*/
int main()
{


    return 0;
}